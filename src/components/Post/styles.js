import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  post: {
    padding: 20,
    marginBottom: 20,
    borderRadius: 10,
    backgroundColor: '#fff',
  },
  postHead: {
    marginBottom: 10,
  },
  postTitle: {
    fontSize: 16,
    fontWeight: 'bold',
  },
  postDescription: {
    paddingTop: 10,
    borderTopWidth: 1,
    borderTopColor: '#DDD',
  },
});
