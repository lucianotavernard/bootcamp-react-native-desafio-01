import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  header: {
    flex: 1,
    justifyContent: 'center',
    paddingVertical: 30,
    marginBottom: 20,
    backgroundColor: '#fff',
  },
  headerText: {
    fontWeight: 'bold',
    textAlign: 'center',
  },
});
